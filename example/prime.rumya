let isPrime n =
begin
    if n < 2 then fault
    else if n < 4 then okay
    else
    for count in 2 ~ (n ^ 0.5 + 1) do
        if n % count == 0
            then fault.
    okay
end.

print "Primality checker".
while let i = input ">> " as num loop
print if isPrime i
    then "YES! prime number"
    else "NO, diviable number"
