TRUE as num := 1.

let isPrime n =
begin
    n > 1.
    if n < 4 then
        TRUE
    else
        for count in
        2 ~ (n ^ 0.5 + 1) do
            n % count != 0.
    TRUE
end.

let max = (input ">> ") as num + 1.
for i in 0 ~ max do
    if isPrime i then
        print i
