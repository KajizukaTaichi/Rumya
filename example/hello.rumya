let messageGen name = "Hello, " + name + "! ".
nameList := [
    { name: "world" },
    { name: "Rumya", repeat: 3 }
].

let index as num = 0.
while let elm = nameList 's index loop
begin
    if let { name, repeat } = elm then
        print messageGen name * repeat
    else if elm =: { name } then
        print name |> messageGen.
    index += 1
end.
