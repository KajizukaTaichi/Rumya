let messageGen name = "Hello, " + name + "! ".
let index: num = 0.
nameList := ["world", { name: "Rumya", repeat: 3 }].

while let item = nameList 's index loop
begin
    if let [
        ["name", name],
        ["repeat", repeat]
    ] = item as arr then
    begin
        messageGen name =: greeting.
        print greeting * repeat
    end
    else
        print messageGen item.
    index += 1
end.
